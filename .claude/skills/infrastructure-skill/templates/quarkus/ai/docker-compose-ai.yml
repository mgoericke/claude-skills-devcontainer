# Docker Compose – AI stack for Quarkus LangChain4j applications
# Co-Author: Claude (claude-sonnet-4-6, Anthropic)
#
# Services:
#   - Application (Quarkus JVM)
#   - PostgreSQL 17 with PgVector extension (database + vector store)
#   - Ollama (local LLM – optional, if OpenAI/Anthropic is used)
#
# Start: docker compose -f docker-compose-ai.yml up -d

services:

  {{ARTIFACT_ID}}:
    build:
      context: .
      dockerfile: src/main/docker/Dockerfile.jvm
    ports:
      - "8080:8080"
    environment:
      # ── Database ──
      QUARKUS_DATASOURCE_JDBC_URL: jdbc:postgresql://postgres:5432/{{ARTIFACT_ID}}db
      QUARKUS_DATASOURCE_USERNAME: app
      QUARKUS_DATASOURCE_PASSWORD: app
      # ── LLM Provider (configure only ONE) ──
      # Option A: OpenAI
      QUARKUS_LANGCHAIN4J_OPENAI_API_KEY: ${OPENAI_API_KEY:-demo}
      # Option B: Ollama (local)
      #QUARKUS_LANGCHAIN4J_OLLAMA_BASE_URL: http://ollama:11434
      # Option C: Anthropic
      #QUARKUS_LANGCHAIN4J_ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/q/health/live | grep '\"status\":\"UP\"' || exit 1"]
      interval: 30s
      timeout: 5s
      start_period: 30s
      retries: 3
    restart: on-failure

  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_DB: {{ARTIFACT_ID}}db
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d {{ARTIFACT_ID}}db"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Ollama – local LLM (optional, comment out if using cloud API)
  #ollama:
  #  image: ollama/ollama:latest
  #  ports:
  #    - "11434:11434"
  #  volumes:
  #    - ollama_data:/root/.ollama
  #  # GPU access (NVIDIA): uncomment if GPU is available
  #  #deploy:
  #  #  resources:
  #  #    reservations:
  #  #      devices:
  #  #        - driver: nvidia
  #  #          count: 1
  #  #          capabilities: [gpu]
  #  healthcheck:
  #    test: ["CMD-SHELL", "curl -sf http://localhost:11434/api/tags || exit 1"]
  #    interval: 15s
  #    timeout: 5s
  #    start_period: 30s
  #    retries: 5

volumes:
  postgres_data:
  #ollama_data:
