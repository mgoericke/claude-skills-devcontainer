/**
 * Fallback handler for the AI Service during LLM outages.
 *
 * <p>Created with Quarkus 3.x · BCE architecture (Control layer / AI)
 *
 * <p>Called when the LLM call fails despite retries.
 * Returns a user-friendly error message.
 *
 * @author Development Team
 * @author Co-Author: Claude (claude-sonnet-4-6, Anthropic) – generated via java-scaffold-skill
 */
package {{GROUP_ID}}.control.ai;

import io.quarkus.logging.Log;
import org.eclipse.microprofile.faulttolerance.ExecutionContext;
import org.eclipse.microprofile.faulttolerance.FallbackHandler;
import jakarta.enterprise.context.ApplicationScoped;

@ApplicationScoped
public class {{AI_SERVICE_NAME}}Fallback implements FallbackHandler<String> {

    @Override
    public String handle(ExecutionContext context) {
        Log.errorf("AI Service unreachable: %s", context.getFailure().getMessage());
        return "The AI assistant is currently unavailable. Please try again later.";
    }
}
