/**
 * AI Service with RAG (Retrieval Augmented Generation).
 *
 * <p>Created with Quarkus 3.x · BCE architecture (Boundary layer / AI)
 *
 * <p>This service uses a {@code RetrievalAugmentor} to enrich responses with
 * domain-specific knowledge from a vector database (PgVector).
 *
 * <p><b>Auto-Wiring:</b> The {@code Supplier<RetrievalAugmentor>} CDI bean
 * (see {@code {{RAG_RETRIEVER_CLASS}}}) is automatically detected by Quarkus LangChain4j
 * and assigned to this service. No explicit configuration needed.
 *
 * @author Development Team
 * @author Co-Author: Claude (claude-sonnet-4-6, Anthropic) – generated via java-scaffold-skill
 */
package {{GROUP_ID}}.boundary.ai;

import dev.langchain4j.service.SystemMessage;
import dev.langchain4j.service.UserMessage;
import io.quarkiverse.langchain4j.RegisterAiService;
import jakarta.enterprise.context.ApplicationScoped;

@ApplicationScoped
@RegisterAiService
public interface {{AI_SERVICE_NAME}} {

    @SystemMessage("""
            You are an expert for {{DOMAIN_DESCRIPTION}}.
            Answer questions based on the provided documents.
            If the documents do not contain an answer, say so honestly.
            Cite relevant passages from the documents.
            """)
    String chat(@UserMessage String question);
}
