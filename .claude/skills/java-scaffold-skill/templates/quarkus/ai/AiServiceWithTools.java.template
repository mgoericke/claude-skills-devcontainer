/**
 * AI Service with tool integration (Function Calling).
 *
 * <p>Created with Quarkus 3.x · BCE architecture (Boundary layer / AI)
 *
 * <p>This service integrates tools that the LLM can call as needed.
 * Tools are registered as CDI beans and referenced via {@code @ToolBox}.
 *
 * @author Development Team
 * @author Co-Author: Claude (claude-sonnet-4-6, Anthropic) – generated via java-scaffold-skill
 */
package {{GROUP_ID}}.boundary.ai;

import dev.langchain4j.service.SystemMessage;
import dev.langchain4j.service.UserMessage;
import io.quarkiverse.langchain4j.ToolBox;
import io.quarkiverse.langchain4j.RegisterAiService;
import jakarta.enterprise.context.SessionScoped;

import {{GROUP_ID}}.control.ai.{{TOOL_CLASS_NAME}};

@SessionScoped
@RegisterAiService
public interface {{AI_SERVICE_NAME}} {

    @SystemMessage("""
            You are a helpful assistant for {{DOMAIN_DESCRIPTION}}.
            You have access to tools that you can call as needed.
            Use the tools to provide precise answers.
            """)
    @ToolBox({{TOOL_CLASS_NAME}}.class)
    String chat(@UserMessage String userMessage);
}
