/**
 * RabbitMQ-Consumer für {{ENTITY_NAME}}-Nachrichten.
 *
 * <p>BCE-Architektur: Boundary-Schicht (boundary/messaging)<br>
 * Empfängt Nachrichten aus der Queue und delegiert an den Service.
 * Queue-Name wird über Property konfiguriert (siehe application.properties).
 *
 * @author Development Team
 * @author Co-Author: Claude (claude-sonnet-4-6, Anthropic) – generiert via java-scaffold-skill
 */
package {{GROUP_ID}}.boundary.messaging;

import {{GROUP_ID}}.control.{{ENTITY_NAME}}Service;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.amqp.rabbit.annotation.RabbitListener;
import org.springframework.stereotype.Component;

@Component
public class {{ENTITY_NAME}}Consumer {

    private static final Logger log = LoggerFactory.getLogger({{ENTITY_NAME}}Consumer.class);

    private final {{ENTITY_NAME}}Service {{ENTITY_NAME_LOWER}}Service;

    public {{ENTITY_NAME}}Consumer({{ENTITY_NAME}}Service {{ENTITY_NAME_LOWER}}Service) {
        this.{{ENTITY_NAME_LOWER}}Service = {{ENTITY_NAME_LOWER}}Service;
    }

    @RabbitListener(queues = "${rabbitmq.queue.{{ENTITY_NAME_LOWER}}}")
    public void handle(String message) {
        log.info("Nachricht empfangen für {{ENTITY_NAME}}: {}", message);
        // TODO: Nachricht verarbeiten und an Service delegieren
    }
}
