/**
 * Guardrails for AI Services – validates inputs and outputs.
 *
 * <p>Created with Quarkus 3.x · BCE architecture (Control layer / AI)
 *
 * <p>Guardrails are implemented as CDI beans and registered on the AI Service
 * via {@code @InputGuardrails} / {@code @OutputGuardrails}.
 *
 * <p>Since quarkus-langchain4j 1.x, the upstream LangChain4j classes
 * from {@code dev.langchain4j.guardrail} are used (no longer {@code io.quarkiverse}).
 *
 * @author Development Team
 * @author Co-Author: Claude (claude-sonnet-4-6, Anthropic) – generated via java-scaffold-skill
 * @see <a href="https://docs.quarkiverse.io/quarkus-langchain4j/dev/guardrails.html">Guardrails Docs</a>
 */
package {{GROUP_ID}}.control.ai;

import dev.langchain4j.guardrail.GuardrailResult;
import dev.langchain4j.guardrail.InputGuardrail;
import dev.langchain4j.guardrail.InputGuardrailParams;
import jakarta.enterprise.context.ApplicationScoped;
import jakarta.inject.Named;

/**
 * Example input guardrail: validates user input before the LLM call.
 */
@ApplicationScoped
@Named("{{INPUT_GUARDRAIL_NAME}}")
public class {{INPUT_GUARDRAIL_NAME}} implements InputGuardrail {

    @Override
    public GuardrailResult validate(InputGuardrailParams params) {
        String userMessage = params.userMessage().singleText();

        // TODO: Implement validation logic
        // Example: detect prompt injection, check length, etc.
        if (userMessage == null || userMessage.isBlank()) {
            return failure("Empty input is not allowed.");
        }

        return success();
    }
}
