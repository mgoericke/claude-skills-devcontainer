/**
 * AI Tool – called by the LLM via Function Calling.
 *
 * <p>Created with Quarkus 3.x · BCE architecture (Control layer / AI)
 *
 * <p>Tools are CDI beans with {@code @Tool}-annotated methods.
 * The LLM decides based on the description when to call a tool.
 *
 * <p><b>Important:</b> When accessing the database within a tool, always
 * use {@code @Blocking} and {@code @Transactional}.
 *
 * @author Development Team
 * @author Co-Author: Claude (claude-sonnet-4-6, Anthropic) – generated via java-scaffold-skill
 * @see <a href="https://docs.quarkiverse.io/quarkus-langchain4j/dev/function-calling.html">Function Calling Docs</a>
 */
package {{GROUP_ID}}.control.ai;

import dev.langchain4j.agent.tool.Tool;
import io.smallrye.common.annotation.Blocking;
import jakarta.enterprise.context.ApplicationScoped;
import jakarta.inject.Inject;
import jakarta.transaction.Transactional;

@ApplicationScoped
public class {{TOOL_CLASS_NAME}} {

    // TODO: Inject services or repositories
    // @Inject
    // {{ENTITY_NAME}}Repository repository;

    @Tool("Description of the function for the LLM – be as precise as possible")
    @Blocking
    @Transactional
    public String {{TOOL_METHOD_NAME}}(String parameter) {
        // TODO: Implementation
        return "Result";
    }
}
