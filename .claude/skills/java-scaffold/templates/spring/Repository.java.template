/**
 * JPA repository for {{ENTITY_NAME}}.
 *
 * <p>BCE architecture: Entity layer<br>
 * Spring Data JPA generates the standard implementation automatically.
 * Add custom queries as {@code @Query} methods or using method name conventions.
 *
 * @author Development Team
 * @author Co-Author: Claude (claude-sonnet-4-6, Anthropic) – generated via java-scaffold-skill
 */
package {{GROUP_ID}}.entity;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.UUID;

@Repository
public interface {{ENTITY_NAME}}Repository extends JpaRepository<{{ENTITY_NAME}}, UUID> {

    // TODO: Add custom queries, e.g.:
    // List<{{ENTITY_NAME}}> findByStatus(String status);
    // @Query("SELECT e FROM {{ENTITY_NAME}} e WHERE e.createdAt >= :since")
    // List<{{ENTITY_NAME}}> findCreatedSince(@Param("since") LocalDateTime since);
}
