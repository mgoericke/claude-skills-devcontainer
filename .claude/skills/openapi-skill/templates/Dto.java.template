package {{GROUP_ID}}.entity.dto;

import jakarta.validation.constraints.*;

/**
 * DTO für {{SCHEMA_NAME}}.
 *
 * <p>Generiert aus OpenAPI-Spec: {{SPEC_FILE}}, Schema: {{SCHEMA_NAME}}<br>
 * Immutables Transfer-Objekt – kein JPA, keine Business-Logik.
 *
 * @author Co-Author: Claude (claude-sonnet-4-6, Anthropic) – generiert via openapi-skill
 */
public record {{SCHEMA_NAME}}(
    {{FIELDS}}
) {}
